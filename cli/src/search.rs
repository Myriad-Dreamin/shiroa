use std::path::Path;

use crate::project::Project;

impl Project {
    pub fn index_search(&self, description: &str, page_path: &Path) {
        let _ = description;
        let _ = page_path;
    }
}
